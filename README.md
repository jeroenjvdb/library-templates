This is a testproject for templating for libraries

# Running the examples

prepare the build directory 

```
cmake -B build src
```

build the project

```
cmake --build build
```

Run the tests

```
cmake --build build --target test
```